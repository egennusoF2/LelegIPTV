<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.app" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!-- Launch-time splash. Bridges the cold-start gap before the WebView is up;
         on Android 12+ this is the native SplashScreen API, on 24-30 it's a
         themed window. installSplashScreen() in MainActivity restores Theme.app. -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_bg</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.app</item>
    </style>
</resources>
